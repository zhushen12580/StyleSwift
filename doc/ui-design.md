# UI 界面设计

## 设计约束

Chrome Side Panel 宽度约 **360-420px**，高度跟随浏览器窗口。

所有界面采用**单列纵向流**布局，不做横向多列分割。

---

## 视图结构

| 视图 | 触发条件 | 说明 |
|------|---------|------|
| 首次引导页 | 无 API Key | 配置引导 |
| 聊天主界面 | 默认视图 | 核心交互 |
| 会话下拉面板 | 点击顶栏会话标题 | 会话管理 |
| 设置页 | 点击顶栏齿轮图标 | 配置管理 |

---

## 聊天主界面

### 布局结构

```
┌─────────────────────────────┐
│  ① 顶栏                      │  ← 固定，~44px
├─────────────────────────────┤
│  ② 风格技能快捷区             │  ← 固定，可折叠
├─────────────────────────────┤
│                             │
│  ③ 对话区                   │  ← 自适应，可滚动
│                             │
├─────────────────────────────┤
│  ④ 操作确认浮层（条件出现）   │
│  ⑤ 输入区                   │  ← 固定底部
└─────────────────────────────┘
```

---

## 组件设计

### 顶栏

```
┌─────────────────────────────────────────┐
│ 🟢  github.com · 深色模式调整  ▾    ⚙️  │
└─────────────────────────────────────────┘
```

| 元素 | 说明 |
|------|------|
| 状态指示灯 | 🟢 就绪 / 🟡 处理中 / 🔴 错误 / ⚪ 受限页面 |
| 域名 | 当前页面域名 |
| 会话标题 | 点击展开会话下拉面板 |
| 设置图标 | 进入设置页 |

### 风格技能快捷区

横向滚动的 chip 列表：

```
┌─────────────────────────────────────────────┐
│ 🎨 [🌙 深色模式] [✨ 极简风] [🔮 赛博朋克] ▸ │
└─────────────────────────────────────────────┘
       ↑ 内置技能           ↑ 用户技能    ↑ 横向滚动
```

| 规则 | 说明 |
|------|------|
| 排列 | 内置技能在前，用户技能在后 |
| 视觉区分 | 内置用实心，用户用描边 |
| 点击行为 | 自动填入"应用 [技能名] 风格"并发送 |
| 长按 | 用户技能支持：应用/查看详情/删除 |

### 对话区

**用户消息**：右对齐气泡

**Agent 文本回复**：左对齐，流式逐字展示

**工具调用卡片**：可交互折叠

```
处理中：
┌──────────────────────────────────────┐
│ 🔧 查看页面结构              ◌ 进行中  │
└──────────────────────────────────────┘

完成后：
┌──────────────────────────────────────┐
│ ✅ 查看页面结构                   ▸  │
└──────────────────────────────────────┘

展开后：
┌──────────────────────────────────────┐
│ ✅ 查看页面结构                   ▾  │
│ ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄ │
│ 输入: (无参数)                       │
│ 输出:                                │
│ URL: https://github.com/...          │
│ body [bg:#fff; color:#333]           │
└──────────────────────────────────────┘
```

### 操作确认浮层

样式应用后浮现：

```
┌─────────────────────────────────────────┐
│     [ ✓ 确认效果 ]    [ ↶ 全部撤销 ]    │
├─────────────────────────────────────────┤
│  继续调整或描述新需求...        [ ➤ ]    │
└─────────────────────────────────────────┘
```

| 消失条件 | 说明 |
|---------|------|
| 点击确认 | 浮层消失，样式保留 |
| 点击撤销 | 回滚样式，浮层消失 |
| 发新消息 | 隐式确认，浮层消失 |
| 60秒超时 | 隐式确认，浮层消失 |

### 输入区

| 状态 | 展示 |
|------|------|
| 空闲 | "描述你想要的风格..." + 发送按钮 |
| 处理中 | "正在处理中..." + 停止按钮（输入禁用） |
| 受限页面 | "此页面不支持样式修改"（整体置灰） |

---

## 会话下拉面板

点击顶栏会话区域展开：

```
┌─────────────────────────────────────┐
│  github.com 的会话                   │
│ ┌─────────────────────────────────┐ │
│ │ ● 深色模式调整          3月4日   │ │  ← 当前会话
│ │   把背景改成深蓝色...            │ │
│ ├─────────────────────────────────┤ │
│ │   字体优化    3月3日       🗑️   │ │
│ ├─────────────────────────────────┤ │
│ │   护眼配色    3月1日       🗑️   │ │
│ └─────────────────────────────────┘ │
│                                     │
│         ＋ 新建会话                  │
└─────────────────────────────────────┘
```

**删除确认**：hover 显示删除图标，点击弹出确认对话框。

---

## 设置页

```
┌─────────────────────────────────────────┐
│ ← 返回                        设置      │
├─────────────────────────────────────────┤
│  API Key                                │
│  ┌───────────────────────────────────┐  │
│  │ sk-ant-api03-••••••••••••••       │  │
│  └───────────────────────────────────┘  │
│                                         │
│  API 地址                               │
│  ┌───────────────────────────────────┐  │
│  │ https://api.openai.com         │  │
│  └───────────────────────────────────┘  │
│  支持兼容 OpenAI 格式的代理地址          │
│                                         │
│  [验证连接]        如何获取 API Key →    │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  模型                                   │
│  ┌───────────────────────────────────┐  │
│  │ claude-sonnet-4-20250514     ▾    │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  存储用量                               │
│  ████████░░░░░░░░  42% (4.2 / 10 MB)   │
│                                         │
│  [清理历史数据]                          │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  关于 StyleSwift v1.0.0                 │
└─────────────────────────────────────────┘
```

---

## 首次引导页

```
┌─────────────────────────────────────────┐
│                                         │
│            ✦ StyleSwift ✦               │
│                                         │
│     用一句话个性化任意网页的视觉样式       │
│                                         │
│                                         │
│  API Key                                │
│  ┌───────────────────────────────────┐  │
│  │  粘贴你的 API Key...              │  │
│  └───────────────────────────────────┘  │
│                                         │
│  API 地址（可选）                        │
│  ┌───────────────────────────────────┐  │
│  │  https://api.openai.com        │  │
│  └───────────────────────────────────┘  │
│                                         │
│          [ 开始使用 StyleSwift ]         │
│                                         │
│     没有 Key？查看获取教程 →             │
│                                         │
└─────────────────────────────────────────┘
```

---

## 状态联动

| 状态 | 顶栏灯 | 对话区 | 输入区 | 技能区 |
|------|--------|--------|--------|--------|
| 就绪 | 🟢 | 正常 | 可输入 | 正常 |
| 处理中 | 🟡 脉动 | 流式输出 | 禁用 | 禁用 |
| 有样式生效 | 🟢 | 正常 | 浮层出现 | 正常 |
| API Key 无效 | 🔴 | 错误横幅 | 可输入 | 正常 |
| 受限页面 | ⚪ | 居中提示 | 置灰 | 置灰 |

---

## 设计原则

| 原则 | 说明 |
|------|------|
| 单列布局 | 适应 Side Panel 窄宽 |
| 信息层级 | 状态 → 会话 → 对话 → 输入 |
| 渐进展示 | 工具调用默认折叠，按需展开 |
| 即时反馈 | 状态灯、浮层、错误横幅 |
| 减少打断 | 确认浮层可超时消失，不强制操作 |
